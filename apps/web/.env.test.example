# Local/CI test DB (Docker Postgres via repo root docker-compose.yml)
# Used by: `npm run test:ai-plan-builder`
DATABASE_URL="postgresql://programassist:programassist@localhost:5432/programassist?schema=public"

# Ensure dev-only auth bypass for tests.
DISABLE_AUTH="true"

# AI Plan Builder feature flag.
AI_PLAN_BUILDER_V1="1"
NEXT_PUBLIC_AI_PLAN_BUILDER_V1="1"
