import type { IconName } from '@/components/ui/iconRegistry';

export const CALENDAR_ADD_SESSION_ICON: IconName = 'scheduleAdd';

// Calibrated to ~10% smaller without affecting layout (transform doesn't change box metrics).
export const CALENDAR_ACTION_ICON_CLASS = 'scale-[0.9] origin-center';
